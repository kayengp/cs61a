def hailstone(n):
    arr=[]
    while n>1:
        arr.append(n)
        if n%2==0:
            n=n//2
        else:
            n=3*n+1
    else:
        arr.append(1)
    return arr
result=hailstone(8)
print(result)